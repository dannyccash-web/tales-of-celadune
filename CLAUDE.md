# Tales of Celadune — dev notes for Claude sessions

## Non-negotiables (from Danny)

- Web-first, no build step, no frameworks. Must stay 100% Steam-publishable later via Electron/Tauri wrap — never introduce anything that would force a refactor into another engine.
- Keep development clean and streamlined.
- Design spec lives in `docs/Fantasy_RPG_Game.pdf` (6 pages: dialog UI, battle UI, item-received UI, D3 overworld mockup, 4-layer spec, overworld map).

## Deployment (do this after every change set)

1. Commit and push to `main` at https://github.com/dannyccash-web/tales-of-celadune
2. GitHub Pages serves `main` branch root → https://dannyccash-web.github.io/tales-of-celadune/
3. Auth: Danny's GitHub token is stored in the local git remote URL (`git remote -v`) — not in any committed file. Never commit tokens.
4. Always verify the live URL after pushing (Pages can take ~1 min to update).
   **Gotcha found 2026-07-09:** this repo's Pages deploy runs via a GitHub Actions workflow ("Deploy to GitHub Pages"), not the legacy branch-build path — so "the live site is stale" can mean the deploy job itself hasn't run yet, not just CDN cache lag. Normal deploy runs take ~20-30s, but if GitHub Actions is degraded platform-wide (check https://www.githubstatus.com/ — look for "Actions" status), a run can take 15+ minutes to even start, during which repeatedly re-fetching the live URL with cache-busting will keep returning old content no matter how long you wait, because the origin itself hasn't been rebuilt (confirmed via `raw.githubusercontent.com/<owner>/<repo>/main/<path>` returning the correct new content the whole time — that endpoint reads the git blob directly and is unaffected by Pages' build lag). If a push doesn't show up live after ~2 min, check `https://github.com/<owner>/<repo>/actions` for the matching "Deploy to GitHub Pages" run's status/duration before assuming it's a cache problem — don't just sit in a wait-and-refetch loop.
5. **Known quirk of this sandbox's mount of the project folder: it can't unlink (or rename) files** (confirmed: even a file just created with `touch` can't be `rm`'d or `mv`'d). Git relies on lock-file unlinks for `add`/`commit`/`push` (`.git/index.lock`, `.git/HEAD.lock`, `.git/refs/remotes/origin/*.lock`), so once any git command leaves one of these behind (e.g. a run that got interrupted), it's stuck forever and every future git write in the mounted `.git` fails with `Unable to create '.git/....lock': File exists`. `git status`/`log`/`diff` (read-only) keep working fine regardless — it's only writes that break, and a `GIT_INDEX_FILE` override alone isn't enough since HEAD/ref locks aren't affected by it. Reliable workaround: copy `.git` to a normal, writable filesystem (`/tmp`), strip any lock/tmp files from the copy, and point git at it with `--git-dir` while keeping `--work-tree` on the real project folder:
   ```
   WT="<absolute path to the project folder>"
   GD="/tmp/celadune_gitdir_$$_$(date +%s)"   # unique name — /tmp on this sandbox
                                                # has been observed to retain files
                                                # across sessions, don't reuse a name
   cp -r "$WT/.git" "$GD"
   find "$GD" -name "*.lock" -o -name "tmp_obj_*" | xargs -r rm -f
   git --git-dir="$GD" --work-tree="$WT" fetch origin
   git --git-dir="$GD" --work-tree="$WT" reset --soft origin/main   # IMPORTANT — see below
   git --git-dir="$GD" --work-tree="$WT" add -A
   git --git-dir="$GD" --work-tree="$WT" commit -m "..."
   git --git-dir="$GD" --work-tree="$WT" push origin main
   ```
   The `fetch` + `reset --soft origin/main` step matters: the mounted `.git`'s own `refs/heads/main` never gets updated by this whole workaround (only the copy's ref and the remote do), so on the *next* session the copy you make will start from a stale local `main` — a plain `commit` on top of it diverges from what's actually on `origin/main` and `push` gets rejected ("fetch first"). `reset --soft` rewinds the copy's branch pointer to the real remote tip while leaving the index/working tree (i.e. your actual file changes) untouched, so the following `commit` lands correctly on top of history that matches origin. Safe to run every time, even if nothing was stale.
   This leaves the mounted `.git` itself stale (still has its own stuck locks, now behind the new commit) — that's fine, it's only used read-only going forward from Claude sessions. If Danny works on this repo directly on his Mac, the stuck lock files are just normal Finder-deletable files there (this restriction is specific to how Cowork's sandbox mounts the folder), so `rm .git/*.lock` locally clears them.
   **Gotcha found 2026-07-08:** after `reset --soft origin/main`, `git status`/`ls-files` can show a batch of files that are actually fine on disk and in the target commit's tree as both staged-`D`eleted *and* untracked (`??`) at the same path — a stale/incomplete index carried over from a previous session's copy (confirmed via `git ls-files | wc -l` being smaller than `git ls-tree -r HEAD --name-only | wc -l`; the missing paths matched exactly). `reset --soft` alone doesn't fix this because if the source index was already short those paths, resetting only moves what's *there*. Fix: run `git read-tree HEAD` right after the reset (rewrites the index to exactly match HEAD's tree, touches nothing in the working directory) — then re-check `git status` before `add -A`/`commit`. Cheap enough to just always run after `reset --soft`, even if nothing was stale.

## Design rules (from the PDF, page 5)

- Fixed 1920×1080 design resolution, scaled to window (see `ui.fitStage`).
- Scenes are 1920×1920; camera scrolls vertically only, clamped to edges.
- Player/NPC icons ~40px; icon bottom = front, faces travel direction.
- Arrow keys move, spacebar interacts. Paths/dirt/grass navigable; buildings, trees, fences, NPCs are obstacles.
- Building entrances sit where the building meets the path.
- Vignette.jpg multiplied over the world, fixed. UI 50px in from edges.
- Overworld 4×4: A1 School of Sorcery, A2/A3 Mountains, A4 Wizard's Fortress, B1 Grassland, B2 King's Castle, B3 Town, B4 Woods, C1 Village, C2 Grassland, C3/C4 Woods, D1 Grassland, D2 Village, D3 Farm ✅ built, D4 Woods.
- Scene transitions preserve position (exit D3 west mid-path → enter D2 at same path point on its east edge).

## Styling (matched to the PDF — keep consistent)

- Fonts (Google): MedievalSharp = HUD labels, gold count, Inventory/Menu, NPC name headings + canvas name labels. Metamorphous = dialog body, response options (selected #fff, unselected #a6a8ab), diamond icon letters. Lato = role caps (semibold #f3d19b, letter-spaced) and bar values.
- Gold frames are gradients (see css vars --frame-light/--frame-dark, --gold-bright/--gold-dark). Dialog boxes: 2px inner + 2px outer rect 9px out. Bars: pointed-hex clip-path with gradient border via padding trick.
- Dialog: 500x500 portrait window bottom-anchored right, image 500 wide top-justified; responses left-justified with the gold SVG arrow sliding in its own column.
- Icons: 3px multiply drop shadow to bottom-right, fixed angle regardless of rotation (canvas silhouette cache in world.js). Shadow alpha is SHADOW_ALPHA in world.js (0.46).
- NPCs steer around player/obstacles (side-committed steering, step+lookahead). Test logic headless in node: `new World({getContext:()=>({})}, scene, {})`, then `update()` in a loop. `window.world` is a live debug handle.
- Testing live: Pages caches JS for 10 min — `fetch(f, {cache:'reload'})` each module, then reload. rAF freezes when the browser window is hidden (drive `world.update` manually under automation).

## Gameplay conventions

- Player speed 130 px/s; icon mirrors horizontally every 0.25s while moving (walk effect) — see WALK_FLIP_INTERVAL in world.js. Same flip applies to NPCs (`npc.moving`/`npc.walkTimer` set in `updateNpcs` by diffing position before/after each tick, since movement happens across several branches — fading/paused/waiting NPCs don't flip). Footsteps loop plays while walking (`audio.setWalking(bool)` each frame, player only).
- Door SFX plays when NPCs leave/return home AND when the player enters/exits an interior.
- Building labels: `buildings: [{label, x, y, r}]` in scene data — drawn on canvas when player within r, centered directly over the building's door (same x as the door, y offset toward the interior — flipped to *below* the door for north-facing doors like Old Barn's). Names on D3: Mirelle's Farmhouse, Tuckwell's House (was Hay Barn), Brenna's House (was Tool Shed), Storehouse, Well, Silo, Old Barn. No "Animal Pen" label — Gaffer lives there and the player just walks up to him instead.
- Soundtrack: js/audio.js, one looped track at a time, `audio.play(src, fadeMs)` cross-fades. Start screen plays celadune_theme.mp3 (retried on first gesture — browser autoplay), Start button cross-fades to celadune_overworld.mp3. Battle/scene tracks later use the same call. **Race condition fixed 2026-07-08:** both tracks could end up playing at once if the very first gesture was the same keypress that both unblocked the theme's autoplay retry *and* triggered Start — `play(theme)`'s success callback (a pending promise) could resolve *after* `play(overworld)` had already run synchronously in the same tick, and its unconditional `fade(el, musicVolume, fadeMs)` would re-fade the theme back in, clobbering the overworld transition's fade-out/pause on that same element (`fade()` cancels the other's `setInterval` via `el._fade`, so whichever fade call resolves later wins). Fixed by guarding the success callback the same way the existing failure `.catch()` already did: only fade in if `current.el === el` still holds, i.e. this track hasn't since been superseded by a newer `play()` call. Keep this guard if `play()` is ever refactored — it's the only thing preventing two tracks from both winning their fades.
- Collision maps are hand-traced from a 25px grid overlay on the scene background (PIL script: 25px pink lines, 50px cyan, 100px red, labels every 100px; inspect per-quadrant crops). CONSERVATIVE RULE (from Danny): if any part of an object touches a 25px cell, the whole cell is blocked — all rect edges are multiples of 25. The well rect is x325-400, y775-900 (frame + stone base only, corrected from an earlier trace that over-extended to y925 and ate into the path); the full path band (y900-985) below it is clear, no detour needed.
- Each of the 4 main crop fields (upper-left, upper-right, lower-left, lower-right) is split into 2 rects with a ~50px walkable dirt lane between them (row-to-row furrows are real but too narrow for the 36px collider even where clear — only wider worker-path-style gaps are worth opening). Lane positions were found by color-classifying plant vs. dirt pixels per column (green/red plant colors vs. tan/brown dirt) and picking the cleanest ~50px window, then eyeballed against a cropped zoom to confirm before committing — pure per-cell auto-detection was too noisy (different crops have different foliage density/color). Lanes: upper-left x275-325, upper-right x1425-1475, lower-left x250-300, lower-right's field west edge pulled in to x1350 (joins the pen/silo corridor below instead of a separate lane).
- Silo access: the pen (x1050-1250, y1000-1225 — see the retrace note below), the lower-right field (starting x1350 instead of x1300), and the grass south of the pen all leave the player multiple ways down to the silo: the ~55-100px corridor at x1250-1350 (main path down past the pen's east side), and now also the ground directly south of the pen (x1050-1175, y1225-1325, between the pen and the barn). That southern pocket is bounded on 3 sides (pen/silo/barn) and is exactly the spot CLAUDE.md previously warned not to open, because NPC steering can wander in and get livelocked (escaping needs a turn sharper than steer()'s ~126° limit) — confirmed again 2026-07-07 when opening it dropped Tuckwell's throughput from ~70 loops/10,000s to 8. It's now open for the player only: obstacles can carry an `npcOnly: true` flag (checked in `world.js`'s `blockersAt`) that's skipped for the player but still blocks every NPC, so the pocket is walkable for the player but invisible-walled for NPCs, none of whom ever legitimately path through it anyway. **Keep this pattern for any future "let the player somewhere NPCs shouldn't wander" fix** — it's safer than physically resizing obstacles NPCs rely on for containment.
- The pen itself is hollow, not solid: 4 thin (25px) fence rects around the x1050-1250, y1000-1225 footprint, with a clear interior at x1075-1225, y1025-1200 for Gaffer (the goat) to wander in. Keep any future pen edits hollow the same way — a solid pen rect would trap Gaffer's patrol targets inside an obstacle. **Retraced 2026-07-07:** the original trace (south rail at y1275-1300) made the collision box ~75px taller than the actual fence art (confirmed via a pixel-grid crop of D3_Background.jpg — the real fence sits at roughly y1000-1200), which let Gaffer visibly wander south of the real fence line before ever tripping a collision. Also updated Gaffer's 3rd patrol point from (1150,1240) to (1150,1160) to stay inside the corrected interior.
- Homes: NPC `home: {door, interior}` + `routine` steps (leaveHome/goto/wait/goHome) in scene data. `startsHome` (explicit bool on the NPC) decides whether they spawn inside — falls back to "routine[0] is leaveHome" if omitted, so old data still works. NPCs that spawn out in the world (Tuckwell, Brenna) set `startsHome: false` and their own `x/y`. Leaving/entering = door SFX + 0.7s fade (FADE_S). Spacebar at door: locked toast if NPC away, dialog over interior background if home (world.interior swaps Layer 1). NPCs at home don't render, collide, or take dialog focus.
- SFX are one-shots via `audio.sfx(src)` (no-op in node); soundtrack unchanged (one looped track, crossfade). Player must only walk paths/grass/dirt: crop fields, hedges, trees, buildings, barrels, well, pen, silo are all rects in js/data/<scene>.js.
- D3 NPCs: Mirelle (home/leaveHome loop: barn → well → home), Tuckwell (Tuckwell's House, formerly labeled Hay Barn; tours all 4 crop fields then home), Brenna (Brenna's House, formerly labeled Tool Shed; pen → silo → home), Old Gaffer (a goat — no home, just `patrol: [{x,y}...]` inside the pen's hollow interior; `world.js`'s existing back-and-forth patrol system, previously unused, covers this). All routine waypoints ride two shared clear corridors — the main path (y900-990 full width) and a couple of 50-75px north-south gaps between buildings (x400-450 by the well, x925-1000 by the barn lane) — never a straight line through a building.
- **Routine waypoint safety margin (important, learned the hard way):** every `goto`/door target needs ≥30px clearance from any obstacle edge, not just "technically not overlapping." COLLIDER is 36px (half=18), so a point only 18-20px clear is one stray avoidance deflection away from a multi-NPC livelock — two NPCs can wedge into a narrow gap and oscillate forever (each "moves" a little every tick, so the stuck-timer fallback never fires, and it never shows up in a short test). Keep shared-corridor waypoints near the center of the y900-990 band (~940) rather than hugging its edges. Validate new waypoints programmatically (check against `scene.obstacles` with the 36px collider) and stress-test with a long headless sim (thousands of simulated seconds, not just tens) before trusting a new route — the Tuckwell/Brenna livelock only appeared after ~400s of sim time.
- Collision test suites live in this workflow: probe walkable/blocked points with `world.blockersAt`, walk exits/lanes with simulated input, and run the 4 NPC steering scenarios (route-around, patrol, overlap-escape, wall-block) headless in node before every collision change. For multi-NPC routine changes, also run a long headless sim (`new World({getContext:()=>({})}, scene, {})`, drive `update()` in a loop for 1000s+ of sim time) checking for zero collision violations and no NPC stuck >3s outside an intended wait/fade/pause.
- Dialog animations: portrait slides in from the right + fades in via a CSS keyframe (`.portrait-enter` on `#dialog-portrait`, class removed + reflowed + re-added on every `openDialog` so it replays). NPC line types out character-by-character (`js/ui.js`, ~22ms/char); pressing Space/Enter while typing completes the line instantly instead of advancing, matching common RPG dialog UX.
- Menu & Inventory panels (`#menu`/`#inventory` in index.html, `.panel-box` styles in style.css, logic in `js/ui.js`): both share one layout — dark backdrop + centered 1500x850 double-gold-framed box (same frame recipe as `.dialog-box`, outer stroke bumped to 4px since these panels are much bigger than the dialog box and the original 2px read thin at that scale), a left vertical tab rail, and a right content area. Opening either pauses the world the same way dialog does (`ui.isAnyPanelOpen()` is OR'd into `main.js`'s `locked` flag); Escape or clicking the dark backdrop close it; opening one auto-closes the other. **No close button** — Danny wants everything playable with no mouse (controller-friendly), so the only ways to close are Escape or re-pressing I/M; see the keyboard-nav bullet below for the full scheme. Menu has Audio (Music/Effects sliders) and Stats (level, XP bar, attack/defense/speed/luck) tabs; Inventory has Equipment (headwear/gloves/armor/boots in a loose paper-doll grid — no character art exists to lay these over, so it's shape-only), Weapons (left/right hand), and Magic (6-slot grid) tabs. There's no equipment system yet, so every Equipment/Weapons/Magic slot renders as an empty gold-diamond `.slot-icon` placeholder (Items tab is a real system — see below). Sliders are custom (native `<input type=range>` made invisible and overlaid on the existing `.bar`/`.bar-fill` hex-clip component + a diamond `.slider-thumb` drawn from the value) rather than a styled native track, so they inherit the same look as the HUD bars for free.
- Audio volume is now live-adjustable: `js/audio.js` exposes `getMusicVolume`/`setMusicVolume`/`getSfxVolume`/`setSfxVolume` (0-1 master levels, defaults 0.75 music / 1.0 effects, matching the sliders' initial 75%/100% — updated 2026-07-07 from the original 80%/90%). `sfx(src, weight)`'s second arg changed meaning from an absolute volume to a relative weight multiplied by `sfxVolume` — if adding new one-shot SFX calls, pass a 0-1 weight, not a target volume.
- Per-character dialogue-open voice clips (2026-07-07): `audio.DIALOGUE_SFX` maps `npc.id` → an mp3 played once, at the moment their dialog opens (a quick audio flourish, not a full voice line). Wired centrally in `main.js`'s `openNpcDialog()`, which every dialog-opening call site (direct approach *and* home-door entry) now goes through, so the clip plays consistently regardless of how the NPC was reached. Add new NPCs' clips here rather than inlining `audio.sfx()` calls at each interact() branch.
- Dialog responses can carry an effect (2026-07-07): `dialog.responseEffects` is an optional array parallel to `dialog.responses` — `null`/omitted entries are inert (the existing placeholder "just closes" behavior), or an entry can be `{ damage: n, message: '...' }`. `ui.openDialog(npc, onClose, onResponse)` now takes a third callback, invoked with `(npc, selectedIndex)` right before the dialog closes; `main.js`'s `applyResponseEffect()` is the one place that interprets these (currently just Gaffer's "Pet Gaffer." response, which bites for 1 damage). Extend this same mechanism for future response effects (item gain, quest flags, etc.) rather than adding parallel logic.
- Health bar width now scales with `healthMax` instead of a fixed CSS size (2026-07-07, from Danny — "it will grow throughout the game as the player advances"): `ui.js`'s `HEALTH_PX_PER_POINT` (30px/point) sets `#health-bar`'s inline width from `stats.healthMax` on every `updateHud()` call. At the new 5-point starting max that's 150px (half the original fixed 300px); a future max-health increase (leveling, gear, etc.) just needs `stats.healthMax` bumped and the bar grows automatically. Magic bar still uses the fixed CSS width since it's hidden and unscaled for now — give it the same treatment if it's ever shown with a variable max.
- Damage flash (2026-07-07): `ui.flashHealthDamage()` (remove/reflow/re-add `.flash-damage` on `#health-fill`, same trick as the dialog portrait's entrance animation) triggers a brief brightness+glow pulse (`@keyframes health-flash` in style.css) layered on top of the fill's existing width transition, so the bar visibly glows while it animates down. Call it alongside `ui.updateHud()` anywhere player health decreases — currently only Gaffer's bite, but any future damage source (battle system, etc.) should do the same pairing.
- **Keyboard-only play (2026-07-07, from Danny — "the whole game should be playable without a mouse," reworked 2026-07-08 into an explicit level hierarchy per his spec):** `I`/`M` toggle Inventory/Menu from anywhere (also switch straight between them, or close, while one's open — see `ui.toggleInventory`/`toggleMenu`). Once a panel is open, navigation is a stack of levels, and **Escape always steps back exactly one level** rather than always closing the whole panel:
  - **Level 0 "tabs"** (the default on open, always the top tab): Up/Down cycle the active tab. Space enters its content (Level 1). Escape closes the whole panel.
  - **Level 1 "content"** (per-tab): Audio — Up/Down move between the Music/Effects rows, Left/Right adjust the focused one by `SLIDER_STEP` (5%). Items — Left/Right move tile focus, Space opens that tile's action popout (Level 2). Stats/Equipment/Weapons/Magic have no interactive content yet, so Level 1 there just waits for Escape. Escape returns to Level 0.
  - **Level 2 "popout"** (Items only): Up/Down move between the popout's enabled actions, Space confirms. Escape closes just the popout, back to Level 1 (the item grid) — unchanged from how this already worked.
  The gold arrow indicator moves with focus: `.panel-tab.active::before` shows only at Level 0 (hidden via a `.content-mode` class ui.js toggles on `.panel-tabs` when entering Level 1), and `.slider-row::before` (a matching gold triangle, new 2026-07-08) shows only on the focused row at Level 1 — so the arrow visibly "hands off" from the tab down into its content on Space, and back up on Escape. All of this lives in `ui.js`'s `panelKey()` + `setNavLevel()`, called from `main.js`'s keydown handler whenever `ui.isAnyPanelOpen()`. Mouse clicks bypass the level state where direct manipulation already works (tab clicks just select+reset to Level 0 since the sliders are draggable regardless of level; item tile clicks set Level 1 then open the popout in one step, since that's the only way to reach it with a mouse) — see the tab/tile click handlers in `initPanels`/`updateItemsPanel`. Any future interactive tab content should extend this same 3-level pattern rather than adding a parallel one.
- Hidden collectibles (2026-07-07): `scene.interactables` in scene data — invisible (no sprite) rects with a `label` and `reward`; same proximity-reveal pattern as building labels (`World.render()` draws the label within `range`, default `INTERACT_RANGE`=90), and `world.nearestInteractableInRange()` + `main.js`'s `interact()` grant the reward once on spacebar (marks `.collected` on the instance, not the scene data, so a fresh `World` always resets). First one: "A shiny object" in the grass north of Mirelle's Farmhouse, `{x:950,y:320,w:80,h:80}`, +3 gold.
- Magic HUD bar is hidden for now (`.stat.hidden` in index.html) — player hasn't learned magic yet. Markup is intact, just toggle the `hidden` class off to bring it back.
- Player starts at 5/10 health and 0 gold (`js/main.js`'s `stats` object) — a deliberate "new game" reset; level/XP/attack/defense/speed/luck are unrelated placeholder flavor values, untouched.
- **Panel chrome redesign (2026-07-07/08, matched to Danny's mockup):** dropped the per-tab-pane `<h2>` section headers from Menu/Inventory; the active tab is now marked by a small gold CSS-triangle arrow (`.panel-tab::before`, opacity toggled by `.active`) sitting to the left of the tab label, instead of the old left-accent bar. `.panel-content` padding tightened (70px/90px → 50px/60px) to use the room the header freed up.
- **Real item system (2026-07-07/08, label convention corrected 2026-07-08):** `js/data/items.js` is the game-wide item *catalog* (id → `{name, image, description, questItem}`), separate from inventory *state* (`js/main.js`'s `inventory` array, `[{id, qty}]`, mutated only via `addItem(id, qty)`/`removeItem(id, qty)`, both of which call `ui.updateItemsPanel(inventory, ITEMS)` to re-render). The Inventory panel's new "Items" tab (`ui.updateItemsPanel`) renders a 5-column tile grid (`.item-grid`/`.item-tile`/`.item-frame`/`.item-label`) from that state each time it changes — each tile shows the item's image, its **name** as the label (kept consistent with how the item is named everywhere else — the received-item reveal, the action popout title), and a `(qty)` suffix when the player has more than one. Inspecting the item (see the popout, below) is what shows its longer `description`. Quest items (`catalog.questItem === true`) get a gold-corner-ribbon + black-star badge (`.quest-badge`, CSS border-triangle trick + a `\2605` `::after` — its offset must stay positive/within the badge's own 34x34 box, since `.item-frame` clips with `overflow:hidden` and a previous negative-offset attempt got silently clipped invisible).
- **Item action popout:** clicking (or Space-selecting, see below) a tile opens `#item-popout` (Use/Inspect/Remove/Cancel) positioned just under that tile — `ui.js`'s `positionPopout()` converts the tile's screen-space `getBoundingClientRect()` into 1920×1080 "stage space" by dividing by the current `#stage` CSS scale factor, since `#item-popout` lives inside `#ui` (which shares that scaled coordinate system). Remove is disabled (`.popout-action.disabled`, via a `data-quest` flag stashed on each tile) for quest items — ui.js reads that flag straight off the tile's dataset rather than importing the catalog itself, keeping ui.js presentation-only. `main.js`'s `onItemAction(itemId, action)` (registered via `ui.initItemsPanel({onAction})`) is the one place that interprets these — Inspect toasts the description, Remove calls `removeItem`, Use is a stub ("Nothing happens... yet.") until real consumables exist. Cancel and outside-clicks just close the popout without calling the handler.
- **Items-tab keyboard nav** extends the existing Menu/Inventory scheme (`ui.panelKey()`) rather than replacing it: on the Items tab, Left/Right walk the tile grid in a flattened row-major order (deliberately not true 2D nav, so it doesn't collide with Up/Down's existing tab-cycling role) and Space/Enter opens the popout for the focused tile. Once the popout is open it takes over the keyboard entirely: Up/Down move between its *enabled* actions, Space/Enter confirms, and Escape closes just the popout (back to grid focus) rather than the whole panel — the one deliberate deviation from "Escape always closes the menu," justified because it's purely additive (there was no popout before) and matches standard modal-within-modal UX. Switching tabs or closing a panel always closes any open popout too (`setActiveTab`/`closePanel` both call `closeItemPopout()`), so it never gets left open pointing at a stale tile.
- **"Received" item reveal (2026-07-08):** `ui.showReceivedItem(itemDef)` plays a gold-down-arrow + "RECEIVED" label + item-frame reveal (`#dialog-received`, `.received-enter` remove/reflow/re-add trick, `@keyframes received-drop`) anchored in the empty right side of the dialog response box, under the portrait — used the moment a `grantItem` response effect fires. `dialog.responseEffects` entries gained a new shape alongside the existing `{damage}`: `{ grantItem: 'item_id', qty?: 1, thankYou?: '...' }`. `main.js`'s `applyResponseEffect()` handles it by calling `addItem`, playing the reveal, and — if `thankYou` is present — calling `ui.updateDialogContent({line, responses})` to swap in a follow-up line/response set *without* closing the dialog box (portrait stays put); it signals this by **returning `true`**, which `ui.chooseResponse()` now checks before auto-closing (previously it closed unconditionally after every response — this is the one behavior change to that function). First live test: Mirelle asks the player to carry a `vegetable_crate` (a quest item) to the tavern; accepting grants it + shows the reveal + a thank-you follow-up line; declining just closes normally.
- Vegetable_Crate.png (and any other binary assets going forward) landed directly in `assets/images/` via Finder rather than a chat upload — **this session's chat-attachment pipeline (zip and inline-pasted images) never synced to any accessible file path**, confirmed repeatedly across multiple re-attempts. If a future session hits the same thing, the reliable fallback is asking Danny to drop the file straight into the connected project folder.
- **Quest system (2026-07-08):** `js/data/quests.js` is the quest catalog (id → `{name, description}`), mirroring the item catalog's pattern. Quest *state* is `js/main.js`'s `quests` array (`[{id, status}]`, status one of `'active'`/`'completed'`/`'failed'`) — `startQuest(id)` is the only way to add one (no-ops if the id's already present, so a quest can never be granted twice) and calls both `ui.updateQuestsPanel(quests, QUESTS)` and `ui.showQuestAdded(def.name)`; `completeQuest(id)` (2026-07-10, first real transition to `'completed'`) mirrors it — panel refresh + `ui.showQuestCompleted` banner (both banners share one `showQuestToast` internal). `dialog.responseEffects` entries gained a `startQuest: 'quest_id'` field alongside `grantItem`/`damage`/`message` — Mirelle's accept response sets both `grantItem: 'vegetable_crate'` and `startQuest: 'vegetable_delivery'` — and (2026-07-10) `addGold: n`, `completeQuest: 'quest_id'`, and `followUp: '...'` (a reply line that keeps the dialog open with a Leave. response — the generic version of grantItem's `thankYou`; used for Brenna's "you're missing out" decline line and the whole Gaffer flow).
- **Quest turn-in via `readyToComplete` pseudo-status (2026-07-10, first user: Brenna's `barn_rat` quest):** `resolveNpcDialog(npc, isQuestReady)` now checks, for an `'active'` quest, whether its world condition is met (`main.js`'s `QUEST_READY` map in `boot()` — `barn_rat`: the `old_barn_rats` encounter's `defeated` flag) and if the NPC has a `readyToComplete` variant in `dialogByQuestStatus`, uses it over the plain `active` one. Brenna's full arc lives in `js/data/d3.js` as data: `none` = the roll-in-the-hay offer (accept → `startQuest` + `followUp`; decline → `followUp`), `active` = "dealt with that rat yet?", `readyToComplete` = thanks + `{addGold: 5, completeQuest: 'barn_rat'}` on the response, `completed` = "the offer stands." Note the quest can be accepted *after* the rat's already dead — the next talk just goes straight to turn-in, deliberately tolerated.
- **Silo corn + happy Gaffer (2026-07-10):** interactables gained `repeatable: true` (never marks `collected` — the silo can be tapped forever, so feeding/discarding corn can't soft-lock the goat), `reward: {item, qty}` (grants via `addItem`), and a custom `message` for the toast; `main.js`'s `interact()` handles all three. The silo's `silo-corn` interactable sits at its north face (`x1270,y1240`, range 130 — reachable from the pen corridor, the path above, and the player-only west pocket; the "Silo" building label draws ~70px south, far enough to coexist). Gaffer's dialog is **state-built, not quest-driven** — `buildGafferDialog(npc)` in `boot()` (routed via `openNpcDialog`'s gaffer branch): unfed + no corn = the plain scene-data dialog (pet = bite, 1 dmg); unfed + corn in inventory = a "Feed Gaffer some corn." response appears (`{feedGaffer: true}` effect: consumes one corn, sets the session-scoped `gafferHappy` flag, swaps the dialog in place); fed = permanently friendly for the session, petting is safe and returns `GAFFER_HAPPY_PET_LINE` flavor instead of damage.
- **One-time quest acquisition via per-status dialog variants:** an NPC's scene data can carry an optional `dialogByQuestStatus: { questId: { active: {...}, completed: {...}, failed: {...} } }` map alongside their normal `dialog`. `main.js`'s `resolveNpcDialog(npc)` checks each entry against the quest's current status (`questStatus(id)`, defaulting to `'none'` before the quest exists) and returns the first matching variant, falling back to `npc.dialog` otherwise; `openNpcDialog()` now always routes through this before calling `ui.openDialog()`. This is how Mirelle avoids handing over a second crate — once `vegetable_delivery` is `'active'`, she asks about its status instead of re-offering it. No `completed` variant exists for her yet since there's no tavern/turn-in scene to actually resolve the quest at — add one (and a real `completeQuest`-style state transition, not yet written since nothing can call it) once that scene exists.
- **"Quest Added" banner (2026-07-08):** `ui.showQuestAdded(name)` shows `#quest-toast`, a gold-framed, top-center banner (`"Quest Added: {name}"`, same spot as the plain `#toast` but MedievalSharp/gold-bright styled and its own drop-in keyframe) that auto-hides after 3.5s. Fires from `startQuest()`.
- **Quests tab (Menu, 2026-07-08):** third Menu tab, read-only (no Level 1 interactive content, so it falls through `panelKey()`'s existing "nothing to focus" branch for free). `ui.updateQuestsPanel(quests, catalog)` lists active quests first (name + description, `.quest-row`), then — only if any exist — a `"Completed"` subhead (`.quest-subhead`) followed by every resolved quest, each with a green check (`status === 'completed'`) or red X (`status === 'failed'`, `.quest-icon.done`/`.quest-icon.failed`). Both completed and failed quests sink under the same "Completed" subhead per Danny's spec (only the icon differentiates them) — verified by hand-feeding fabricated `{status:'completed'}`/`{status:'failed'}` entries through `ui.updateQuestsPanel()` in the browser console, since nothing in-game can actually resolve a quest yet.
- **Centralized gold/health/item SFX (2026-07-08):** `addGold(amount)` and `damagePlayer(amount)` (mirroring the existing `addItem`/`removeItem` pattern) are now the *only* places that touch `stats.gold`/`stats.health` — each plays its own SFX (`audio.SFX.gold`/`.hurt`) alongside the existing HUD/flash updates. `addItem`/`removeItem` also gained `audio.SFX.item`. Route any future gold/health/item change through these four functions rather than mutating `stats`/`inventory` directly, or the sound (and the HUD update) gets silently skipped.
- **Unoccupied buildings ("places"), first one: Your House (2026-07-08):** the building labeled Storehouse is renamed "Your House" and modeled as a place — a special entry in scene data's `npcs` array (`isPlace: true`) rather than a parallel system, since it reuses almost all of the NPC-home machinery for free: `home.door`/`home.interior` for the door-proximity + interior-image swap, and `startsHome: true` with no `routine`/`patrol` so `atHome` stays permanently true (never "away", never rendered as a body, never picked up by `nearestNpcInRange()` — only reachable via its door like any home). `main.js`'s `openNpcDialog()` branches on `npc.isPlace` to `buildPlaceView(place)`/`applyPlaceResponse()` instead of the normal NPC path (no voice clip, no quest-status dialog variants). A place's dialog view: blank role (`ui.openDialog` now defaults `role` to `''` if falsy), `place.description` stands in for the dialogue line, and one `"Take {item name}"` response per id still in `place.items` (plus `Leave.`) — picking one filters that id out of `place.items` (a non-mutating reassignment, since `world.js` already shallow-copies each npc off scene data the same way `.atHome` etc. are instance-scoped, so the original module array is never touched), grants the item via the existing `addItem`/`showReceivedItem` pair (same reveal as Mirelle's crate), and calls `ui.updateDialogContent()` (now also accepts an optional `contents` array) to refresh the room in place. Where a portrait normally goes, `ui.openDialog()` shows `#dialog-contents` instead when `npc.isPlace` (a "Contents" heading + `#contents-list`, same 500×500 slot as `#portrait-frame`, toggled once at open time since a place's dialog session never switches modes) — see `renderContentsList()`. First stock: Dagger (reuses the pre-existing `assets/images/dagger.png` from the original asset drop) and Health Potion (icon pending from Danny, same Finder-drop workflow as Vegetable_Crate.png — path is set to `Health_Potion.png` and the item works fully with a broken image until that lands). Extend this same `isPlace` pattern for any future unoccupied building rather than inventing a new one.

## Battle system (2026-07-09)

- Turn-based combat: Attack/Defense are flat modifiers on a 1d20 roll. `js/battle.js` is pure, headless-testable dice/turn-order math (`rollD20`, `resolveAttack(attackerScore, defenderScore, rng)` — attacker wins strict ties go to defender, `rollDamage(damage, rng)` for flat-or-{min,max} weapon damage, `turnOrder(combatants)` — speed-desc, stable tiebreak by original index). All take an injectable `rng` (default `Math.random`) so tests can be deterministic. `js/data/enemies.js` is the enemy catalog (id → `{name, portrait, health, attack, defense, speed, damage}` — blight_rat: 2hp, atk 1, def 1, spd 8, dmg 1 flat; **replaced the kobold entirely 2026-07-10** per Danny's spec, portrait `Blight_Rat.png` via the Finder-drop workflow; speed 8 was carried over from the kobold, unspecified in the spec. A dagger hit [2 dmg] one-shots a rat).
- Player starts Attack 1 / Defense 1 (`js/main.js` `stats`, replacing the old placeholder 14/9). `effectiveAttack()`/`effectiveDefense()` add up any equipped gear's `attackBonus`/`defenseBonus` (none yet). `weaponDamage()` reads `equipment.mainhand`'s `damage` from the item catalog, defaulting to 1 (unarmed) if nothing's equipped. Dagger = 2 damage, no attack/defense bonus.
- Equip system: `equipment` state object in `js/main.js` (one item id per slot, item stays in inventory — not consumed), slots gated by each item's catalog `slot` field: `'head'|'clothing'|'feet'|'hands'` (Inventory → Equipment tab) or `'mainhand'|'offhand'` (Weapons tab). `equipItem`/`unequipItem` are the only two ways `equipment` changes (mirrors the existing `addItem`/`removeItem`/`addGold`/`damagePlayer` centralized-mutator pattern) — both refresh all four category grids (equipped tag) and the Equipment/Weapons tab slot art, and play SFX. An item tile's action popout primary button is dynamically "Equip"/"Unequip" (was always "Use") based on whether the tapped item has a `slot` and is currently equipped (`ui.js`'s `primaryActionFor`); Remove is disabled for equipped items too, not just quest items. **Reworked 2026-07-09 — see the "Inventory categories" bullet below**, which moved this popout out of a shared Items tab and into each item's own category tab.
- Equipment/Weapons tabs redesigned from placeholder diamond grids into real labelled slots (`data-slot` on each `.item-slot` in index.html) — `ui.updateEquipmentPanel(equipment, catalog)` fills each slot with the equipped item's art or an empty-label fallback (`SLOT_EMPTY_LABEL`). Space on a filled slot unequips it (browsing/choosing what to *equip* happens via that tab's item grid below the slots, not the slot itself — see the "Inventory categories" bullet below for the full keyboard scheme, reworked 2026-07-09 from an earlier version where this was view-only and equipping only happened from a separate Items tab).
- Battle UI (`#battle` in index.html, styled in style.css, all logic in `js/ui.js`'s "Battle" section; **redesigned 2026-07-09 to Danny's battle mockup**): a trapezoid status banner hanging flush from the top screen edge (two nested clip-path trapezoids — gold gradient outside, `--panel-bg` inside, ~3px slanted edges, NOT a `.dialog-box` since border can't follow a clip-path; round-3 geometry per Danny, 2026-07-10: sides at exactly 45° — horizontal inset = the 70px height — with the banner's *TOP* edge aligned to the other frames' 205px insets and the clip pulling the bottom corners inward; round 2 had the bottom edge aligned instead, which read "too wide"), one wide double-framed box of large cropped enemy portraits (per slot: `.enemy-portrait-window`, a fixed 443px tall × up-to-400px wide window per Danny's layout — bottom-anchored 45px up so its top pokes ~70px OUT above the box's frame, children painting over the border; width caps at 400px leaving dark gaps between enemies at 3-up, going fluid when more enemies shrink the slots; `object-fit: cover; object-position: top` so big art crops at the bottom — with a 320px HP bar riding fully on the art near its bottom [bar at bottom:58, art runs to 45] with "cur/max" text inside, 28px Metamorphous name below — `.targeted` = silhouette gold glow + white name; `.defeated` dims), and an action row of **four labeled diamond slots — Main Hand / Off Hand / Use / Flee** (Magic removed 2026-07-10 per Danny, no magic system exists yet — its old div/config are gone from index.html/`showPlayerActions()`, restore both to re-add) — each showing the equipped item's art counter-rotated inside the diamond + a small-caps sub-label. An empty hand slot shows a gold fist icon (`assets/images/icon_fist.svg`) + "UNARMED" and is still a live attack option (1 dmg); Flee shows a running-figure icon (`icon_run.svg`); Use shows whatever Items-category consumable is equipped to the **'item' equip slot** (2026-07-10: `equipment.item`, populated by Equip on e.g. the Health Potion's tile in Inventory > Items — `items.js` gives the potion `slot: 'item'` and maps that slot to the `items` category in `SLOT_CATEGORY` so it stays in the Items tab; consumables also get a second "Use" popout row [`#popout-use`, toggled via the tile's `data-usable`] since Equip took their primary button) as `"{Name} (count)"`, disabled at 0 or when nothing's equipped. Using it in battle consumes one via the shared `usePotion()` and **spends the turn**; `removeItem()` auto-unequips any slot whose item hits qty 0 (so draining the last equipped potion empties the Use diamond by itself). `ui.showBattleActions(slots)` takes per-slot config keyed by data-action (`{sub, image, disabled}`); `ui.setBattleMessage()` auto-wraps any `+N`/`−N` token in green/red spans (mockup's "+1 damage done" / "−2 damage taken") — built with DOM nodes, not innerHTML. Keyboard Left/Right **skip disabled slots** (empty Off Hand, Use at 0 potions; Flee is never disabled, so the skip loop can't spin forever). Two focus sub-modes on the action row vs. target-select (`ui.startTargeting()` — **now returns false** when there's no alive target, and callers must re-show the action row themselves — target Left-Right-cycle-Space-confirm-Escape-cancel), both driven through `ui.battleKey(key)` which `main.js`'s keydown handler routes to whenever `ui.isBattleOpen()` — arrow keys/space/escape only, movement and I/M blocked while battle is open (deliberate scope simplification, same as dialog/panel locking). ui.js reports the picked slot through one `handlers.onAction(action)` callback (replacing the old per-action onSelect* handlers); `main.js`'s `handleBattleAction()` interprets it — mainhand/offhand set `pendingAttackSlot` and enter targeting (consumed by `playerAttack`, whose damage comes from the slot-aware `weaponDamage(slot)`: the equipped weapon's `damage` rolled via `battle.rollDamage`, or 1 unarmed for an empty hand — as of 2026-07-10 *both* hands attack while empty, per Danny), use = the equipped 'item'-slot consumable via `playerUseItem()` (spends the turn only if actually consumed), flee ends the battle. Exactly one action per player turn, per Danny's spec.
- Orchestration lives in `js/main.js`'s `boot()` closure, not `battle.js` (kept pure) or `ui.js` (kept presentation-only): `battleState = {active, enemies, order, turnPos, onEnd}`. `startBattle(enemyIds, onEnd)` builds enemy instances from the catalog and calls `rollRoundOrder()` (speed-based initiative, recomputed fresh each round from currently-alive combatants only — a combatant that died mid-round doesn't reappear in the *next* round's order, but its already-rolled slot in the *current* round can still come up). `runQueue()` walks `battleState.order`, pausing on the player's slot (shows the action menu) and auto-resolving enemy slots after a `ENEMY_TURN_DELAY_MS` (900ms) pacing delay so messages are readable back to back.
  - **Bug fixed 2026-07-09 (real, caught during live testing — not a hypothetical):** an enemy killed by the player earlier in the same round still had its already-rolled turn-order slot come up later that round, and `runQueue()` called `resolveEnemyTurn()` on it with no liveness check — so a dead enemy could still land a hit "from the grave." Live-reproduced: player topped off to full health, one kobold reduced to 0hp via the player's attack, and the player's health still dropped to 0 that same round from the corpse + the two other kobolds. Fix: `runQueue()` now checks `current.enemy.health <= 0` before the `setTimeout`/`resolveEnemyTurn` call and, if dead, advances `turnPos` and recurses immediately (no delay, no message) instead. **Any future change to `runQueue()`/`rollRoundOrder()` must preserve this liveness check** — it's easy to reintroduce by "simplifying" the loop.
  - `checkBattleEnd()` (defeat if `stats.health <= 0`, victory if all enemies dead) runs at the top of every `runQueue()` call. `endBattle('defeat')` shows the Game Over screen and stashes `onEnd` in `pendingDefeatCallback` (fired later by `respawnAfterDefeat`, not immediately — Game Over needs to stay up until the player dismisses it). `endBattle('victory'|'fled')` closes the battle overlay immediately and calls `onEnd(result)` right away.
- Game Over (`#game-over` in index.html): shown via `ui.showGameOver()` on defeat, blocks all input except the "Try Again" button/Enter/Space (`ui.isGameOverOpen()` gates `main.js`'s keydown handler and is OR'd into the `locked` flag alongside dialog/panel/battle). `respawnAfterDefeat()` fully heals (`stats.health = stats.healthMax`), teleports to `scene.spawn`, hides the screen, then fires the stashed `pendingDefeatCallback` — verified live: does NOT mark the triggering encounter `defeated`, so a lost fight can be retried.
- Blight Rat test encounter (id `old_barn_rats`, were kobolds until 2026-07-10): `scene.battles: [{id, door, enemies: [...]}]` in `js/data/d3.js` (three Blight Rats at the Old Barn's door, matching the existing `bottom_barn` entrance rect) + `World`'s `this.battles` (mirrors the existing `interactables` pattern, per-instance `defeated` flag reset on every fresh `World`) + `world.battleNearDoor()` (proximity check, same `INTERACT_RANGE` pattern as home doors/interactables). `main.js`'s `interact()` checks this after the home-door branch and calls `startBattle(trigger.enemies, (result) => { if (result === 'victory') trigger.defeated = true; })` — only victory disables the encounter; fleeing or losing leaves it re-fightable. Tuckwell's dialog line and the victory toast were de-kobolded too (toast is now the generic "Victory! Your foes are defeated.").
- **Battle music (2026-07-10):** `audio.TRACKS.battle` = `celadune_battle.mp3`. `startBattle()` captures `audio.nowPlaying()` into `preBattleTrack` (guarded: not captured if the battle track is somehow already playing, so back-to-back debug starts can't "restore" battle music) and cross-fades to the battle track; `endBattle()` cross-fades back to `preBattleTrack` on **every** outcome — victory, flee, and defeat (the Game Over screen sits over the restored music) — falling back to the overworld track if nothing was playing (autoplay still blocked at battle start). All via the existing `audio.play()` crossfade; `battleDebug.nowPlaying` is exposed for verifying this in automation.
- **Debug handle gotcha (2026-07-09):** the live debug handle for battle testing is `window.battleDebug`, deliberately **not** `window.battle` — the browser auto-globalizes DOM elements onto `window` by their `id` attribute, and `<div id="battle">` (the battle overlay) would silently win that name, shadowing an explicit `window.battle = {...}` assignment with zero error (confirmed: `Object.getOwnPropertyNames(window.battle)` came back empty and `window.battle.constructor.name === 'HTMLDivElement'`). **Any future debug handle must avoid names matching an existing element id** — check `index.html` first. `window.battleDebug` exposes `{start, state, stats, equipment, effectiveAttack, effectiveDefense, weaponDamage, playerAttack, playerFlee, playerUsePotion, playerUseMagic, checkBattleEnd}` for live console/automation testing (mirrors the existing `window.world` debug handle convention).
- `usePotion(itemId)` returns `{ok, message}` rather than firing its own toast, so callers can route the message to the right UI surface — an item tile's popout still uses `ui.toast(...)`, but `playerUsePotion()` (battle context) uses `ui.setBattleMessage(...)` instead. **Reason:** `#toast` and `#battle-status` occupy nearly the same screen region (top:60px vs top:30px/74px tall), so a toast firing while the battle overlay is open visibly overlapped the battle message — confirmed via a live screenshot before the fix. Same treatment applied to `playerUseMagic()`. Keep this toast-vs-battle-message split for any future action usable both in and out of battle.
- **Battle action-menu lockup, fixed 2026-07-09 (real bug, caught live by Danny — "I chose Magic and it locked up the game"):** `ui.battleKey()` sets its internal mode to `'idle'` the instant an action is selected, *before* calling the handler — every handler is expected to either call `runQueue()` again (which re-arms the menu once it's next the player's turn) or explicitly re-show it. `playerUseMagic()` (always a no-op — no spells exist yet) and `playerUsePotion()`'s "you have no potions" path did neither, so the action menu was left with `mode: 'idle'` and nothing left to respond to a keypress ever again — the game visually looked frozen. Fixed with a new `showPlayerActions()` helper (wraps `ui.showBattleActions({weaponName, potionCount})`) called from `runQueue()`'s player-turn branch *and* from both no-op paths. **Any future battle action that can end without spending a turn must call `showPlayerActions()` before returning, or reintroduce this exact bug.**
- **Inventory categories, reworked 2026-07-09 (Danny's spec, from live feedback after the first battle-system test pass):** every inventory item now belongs to exactly one of four top-level categories — Equipment, Weapons, Magic, Items — and renders *only* in that category's Inventory tab; an equippable item like the dagger no longer also shows up in Items (it previously did, which Danny flagged as not matching his design). `js/data/items.js` exports `categoryFor(item)`, deriving the category from the item's existing `slot` field via a `SLOT_CATEGORY` map (`head/clothing/feet/hands` → `'equipment'`, `mainhand/offhand` → `'weapons'`; no `slot` → `'items'`; any other future slot value → `'magic'`, so a real magic-item slot just needs adding to that map) rather than storing category redundantly on each item. Equipment/Weapons/Magic tab-panes in index.html now each pair their equip-slot row with a `.section-subhead`-labeled "Inventory" item grid below it (`#equipment-grid`/`#weapons-grid`/`#magic-items-grid`, same `.item-grid`/`.item-tile` component Items already used) — `ui.js`'s `renderCategoryGrid()` (called four times per `updateItemsPanel()`, once per category) filters `inventory` by `categoryFor()` into each. Equipping now happens directly from a category tab's own grid (open the tile's popout, choose Equip) instead of a separate Items tab. Keyboard nav: Equipment/Weapons have two Level-1 focus zones — the slots row and the grid below — switched with **Up/Down** (`contentZone: 'slots'|'grid'` in `ui.js`, gates `refreshSlotFocus()`/`refreshGridFocus()`), Left/Right move within whichever has focus, Space acts on it (slot → unequip if filled; grid tile → open the popout). Magic/Items have no slots zone, so Up/Down are unused there and their grid is always focused, unchanged from the old Items-tab behavior. `.quest-subhead` was renamed to the generic `.section-subhead` (now also used for these "Inventory" subheads) since it was no longer Quests-tab-specific.
- **A "layout looks wrong" report that turned out to be stale cache, not a bug (2026-07-09):** Danny reported the battle overlay's frames sitting "way high up" with a big gap showing the world bleeding through between the enemies box and the action row. Live re-inspection right after (fresh page load, `getBoundingClientRect()` on every battle box) found completely correct geometry and solid `rgba(12,9,6,0.82)` backgrounds — no gap, no bleed-through, matching the mockup. Given the timing (right after the GitHub Actions outage that made an earlier deploy take 15+ minutes — see the Deployment section above), the most likely explanation is Danny's browser had a stale cached copy of the JS/CSS from that exact window. **Not fully confirmed** (couldn't reproduce, so couldn't verify a fix) — if this specific complaint (large gap, "Old Barn" visible through the battle overlay) recurs after a hard refresh, treat it as a real bug and investigate the `.dialog-box` height/positioning math in style.css's Battle section, not cache, next time.
- **Equipment/Weapons tabs reworked again 2026-07-09 (Danny sent a mockup screenshot; supersedes the "two Level-1 focus zones" design described in the "Inventory categories" bullet above), into a stacked list of subcategory sections — one header (`.category-section-header`, small-caps gold + full-width rule) + one full item grid per equip slot (Head/Clothing/Feet/Hands for Equipment; Main Hand/Off Hand for Weapons), always rendered even when empty ("Nothing here yet."). Standalone equip-slot boxes (`.item-slot[data-slot]`, `.paperdoll`, `.weapon-row`, `ui.updateEquipmentPanel`/`initEquipmentPanel`) are gone entirely — equip/unequip now happens only via a tile's own popout. `items.js` exports `CATEGORY_SLOTS` (which slots belong to which tab, in display order) and `SLOT_LABEL` (slot → header text); `ui.js`'s `renderCategoryTab(tab, inventory, catalog, equipment)` builds the sections, reusing a shared `buildItemTile()` with the flat Magic/Items grids (`renderCategoryGrid`). Keyboard nav: Up/Down now switch which *section* has focus (`sectionIndex`, replacing the old `contentZone`/`slotFocusIndex` pair), Left/Right walk that section's grid, Space opens the popout — Magic/Items are unaffected (single implicit section, Up/Down still unused there).
- **Item tiles gained a secondary stat line (2026-07-09, matches the mockup's "1 DMG" placeholder):** `items.js`'s `statLineFor(item)` returns e.g. `"2 DMG"` for a weapon's `damage`, or `"+N ATK"`/`"+N DEF"` for `attackBonus`/`defenseBonus` if set, else `null` (nothing rendered). Rendered as `.item-stat` under the tile's name/qty line in `buildItemTile()`.
- **Equipped-item badge (2026-07-09):** same gold corner-ribbon component as the quest-item star (`.quest-badge`), now generalized — see the badge-positioning fix two bullets up — with a `.equipped-badge` modifier (black checkmark `✓` instead of a star `★`) applied to any tile where `equipment[def.slot] === entry.id`. Quest and equipped badges are mutually exclusive per tile (an item can't currently be both).
- **Item action popout redesigned (2026-07-09, Danny: "we don't need to see the name of the item there again... it would be cool if the frame from the item could expand downward"):** dropped the `.popout-title` row entirely — `positionPopout()` in `ui.js` now sizes `#item-popout` to exactly match the anchor tile's width (not a fixed `min-width`) and positions it flush beneath the tile; visibility is driven by a `.open` class (CSS `scaleY`+opacity transition, `transform-origin: top`) instead of the shared `.hidden` (`display:none`, which can't transition) so it visually grows out of the tile's own frame. Actions restyled from Metamorphous body text to the game's cream-gold (`--role-gold`) all-caps Lato treatment used for role captions/section headers elsewhere.
- **Quest star badge invisible, root-caused and fixed 2026-07-09 (Danny: "I'm still not seeing the black star... maybe it's behind the triangle"):** the badge used to *be* the 0-sized border-triangle element itself, with the star as its `::after`. A 0×0 box with asymmetric border widths (34px top/right, 0 bottom/left) has a **degenerate padding box** — the CSS containing block for an absolutely-positioned `::after` is the *padding edge*, which for those border widths sits offset from where the triangle is actually painted, not at the element's visual (0,0) corner. That could silently push the glyph outside the painted triangle, landing on the dark frame background where it's invisible — easy to miss since nothing errors and the triangle itself still renders fine. Fixed by giving `.quest-badge`/`.equipped-badge` a real 34×34 box (non-degenerate) and moving the triangle into `::before`, so the glyph (`::after`) positions relative to a normal box. **If a future badge/ribbon needs the "0-sized-element-with-asymmetric-borders-as-a-triangle" trick again, don't add positioned children directly inside it — wrap it in a normal-sized parent first**, same pattern now used here.
- All five of the above were live-verified end to end after deploy (commit `020e6b2`): took the Dagger from Your House (contents position + no description re-typing + item padding confirmed), equipped it from the new Weapons > Main Hand section (checkmark badge appeared), unequipped it via the redesigned popout (badge cleared, `REMOVE` correctly disabled while equipped, `EQUIP`/`UNEQUIP` label toggled correctly), and took Mirelle's vegetable-crate quest item (star badge now clearly visible in the Items tab). Zero console errors across a fresh page load and the full interaction sequence.
- **Dialog polish (2026-07-09, Danny's dialog mockup):** every `.dialog-box`'s outer frame thickened 2px → 4px (`.dialog-box::before`, matching the panels' existing 4px — the battle enemy/action boxes get this for free since they reuse the class). The received-item block (`#dialog-received`) is centered horizontally under the portrait column (`right: 165px` — portrait is right:30 + 500 wide, center 280 in from the box edge, minus half the block's 230px width), and its old block-arrow SVG is now a plain solid gold border-triangle (`.received-caret` — safe standalone since nothing is positioned inside it, per the badge gotcha above). **Overflow fix, caught live:** with a short response list (just "Leave." after accepting Mirelle's crate) the response box was ~140px tall and the ~317px received block hung out past its bottom frame — `#dialog-response-box:has(#dialog-received:not(.hidden)) { min-height: 345px; }` grows the box only while the reveal is showing, so ordinary dialogs stay content-sized.
- **SVG-icon XML gotcha (2026-07-10, cost a debug round live):** the gold fist/run icons (`assets/images/icon_fist.svg`/`icon_run.svg`, hand-written SVGs) initially rendered as broken images despite the server returning 200 + `image/svg+xml` — the files' comments mentioned `--gold-bright`, and **a double hyphen is illegal inside an XML comment**, making the whole SVG unparseable; browsers show the broken-image glyph with no console error. `fetch().text()` looks fine, which is what makes it sneaky — validate with `python3 -c "import xml.dom.minidom; xml.dom.minidom.parse('file.svg')"` before pushing any hand-written SVG. Related: Chrome caches the *failed* image for the plain URL, so even after the fix the icon stays broken until the HTTP cache entry is refreshed (`fetch(src, {cache:'reload'})`, then reload) — don't mistake that for the fix not working.
- **Your House contents column (round 2, 2026-07-10):** `#dialog-contents` is now TOP-anchored (top: 41px, optically aligning the CONTENTS heading with the place-name h2 across the box's 30px padding) instead of the old bottom-anchored flex-end layout — the heading never moves, the item list stacks top-justified under it, and since `renderContentsList()` already re-renders from `place.items` on every take, a taken item disappears and the rest reflow up automatically.
- **Sandbox network gotcha (2026-07-09):** the shell sandbox's proxy can silently break deploy verification — `curl` to `dannyccash-web.github.io` with a `?cachebust` query string returned a 403 `X-Proxy-Error: blocked-by-allowlist` **whose body greps as 0 matches, indistinguishable from "the deploy isn't live yet"**, and `raw.githubusercontent.com` was fully unreachable (HTTP 000). Burned several minutes of false "deploy is stale" retries. Verify deploys from inside the game's own origin instead: Claude-in-Chrome on the live URL + `await (await fetch(f, {cache:'reload'})).text()` and grep in JS — that's also the documented cache-busting flow for live testing anyway.

## Status / roadmap

- ✅ D3 Farm: 4-layer scene, movement, collision (25px-grid traced), camera, walk animation, four NPCs (Mirelle, Tuckwell, Brenna on home routines; Old Gaffer the goat on a patrol loop in the pen) plus one unoccupied building (Your House, formerly Storehouse, stocked with a Dagger + Health Potion) — leave/return + door SFX + interior dialog with per-character voice-clip SFX, response effects (Gaffer's bite, Mirelle's vegetable-crate quest + item, Brenna's completable barn-rat quest with a 5-gold turn-in, the silo's repeatable corn + feeding Gaffer to make petting safe), per-NPC dialog variants by quest status incl. the readyToComplete turn-in pseudo-status (no re-granting a one-time quest item), multi-NPC steering avoidance, dialog with portrait slide/fade + typewriter text + item-received reveal (also used for taking items from Your House), PDF-matched UI styling, HUD (Magic bar hidden; health bar width scales with max-health and flashes on damage), gold/health/item SFX centralized through addGold/damagePlayer/addItem/removeItem, "Quest Added" top-center banner, Menu (Audio/Stats/Quests tabs — Quests lists active + a Completed section with check/X icons) + Inventory (mockup-matched chrome, no section headers, arrow-indicator tabs; four mutually-exclusive item categories — Equipment/Weapons/Magic/Items — each with its own tile grid + action popout; Equipment/Weapons further split into per-slot subcategory sections — Head/Clothing/Feet/Hands, Main Hand/Off Hand — each its own header+grid, equip/unequip via the tile's own expand-from-frame popout, equipped items marked with a checkmark corner badge, quest items with a star badge, weapon/gear tiles show a secondary stat line, consistent item naming), fully keyboard-navigable (I/M/arrows/Space/Escape, no mouse required, including the Items grid + popout and battle), one hidden collectible ("A shiny object" near Mirelle's farmhouse), start screen (click/Enter/Space), theme + overworld soundtrack with crossfade (race-condition-free).
- ✅ Battle system: d20+modifier turn-based combat (see "Battle system" section above), speed-based initiative, player-picks-target, one action per turn via four mockup-matched diamond slots (Main Hand/Off Hand/Use/Flee — Magic removed until a magic system exists; empty hands attack unarmed with a gold fist icon, Use fires the equipped 'item'-slot consumable, Flee wears a runner icon), colored +N/−N damage numbers in the trapezoid status banner, large cropped enemy portraits with overlapping HP bars, one Blight Rat at the Old Barn door as the test encounter (2026-07-10; was three, and kobolds before that), battle-music crossfade in/out of `celadune_battle.mp3`, Game Over + retry on defeat, victory/flee both leave the world unlocked. Live-tested end to end after the 2026-07-09 redesign (targeting glow, disabled-slot skipping, Magic free no-op re-arming the menu, flee, forced victory with "falls!" messages, organic defeat → Game Over → respawn) with zero console errors.
- ⏳ Later: real dialog trees, ranged/varied enemy types beyond the Blight Rat test encounter, an actual Magic system behind the Menu's hidden bar and the battle UI's Magic button (currently a "you don't know any spells yet" stub), gear that grants attackBonus/defenseBonus (none exists yet — only weapon damage is wired), more items/collectibles/unoccupied buildings, interiors, other 15 scenes, Steam wrap.
- Player stats currently hardcoded in `js/main.js` (health 5/5, magic 5/10 [bar hidden], gold 0, level 4, XP 1,240/2,000, attack 1, defense 1, speed 11, luck 6 — health/gold/attack/defense are deliberate new-game-start values per Danny's battle spec; level/XP/speed/luck are unrelated placeholder flavor, informed guesses). Audio defaults: music 75%, effects 100%. Inventory state (`js/main.js`'s `inventory` array) starts empty and `equipment` (new, 2026-07-09) starts with every slot unequipped — first items obtainable in-game are Mirelle's vegetable-crate quest and whatever's still in Your House (Dagger, Health Potion).
